'use strict';

// document.body.addEventListener('mouseover', (evt) => {
//     const target = evt.target;
//     if (target.contains('info__icon') && window.innerWidth > 991) {
//         target.closest('.info__icon').nextElementSibling.classList.add('info__content_opened');
//     }
// });
// document.body.addEventListener('mouseout', (evt) => {
//     const target = evt.target;
//     if (target.contains('info__icon') && !evt.relatedTarget.contains('info__icon') && window.innerWidth > 991) {
//         target.closest('.info__icon').nextElementSibling.classList.remove('info__content_opened');
//     }
// });